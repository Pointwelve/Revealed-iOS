query queryAllConfigs {
  getAllTopics {
    edges {
        id
        name
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }

  getAllTags {
    edges {
        id
        name
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
