os: osx
language: objective-c
osx_image: xcode11.3
cache:
  directories:
  - Carthage
before_install:
- brew bundle
- npm install -g apollo
- carthage bootstrap --platform iOS --cache-builds --new-resolver
install:
- bundle install
script:
- sh build.sh
branches:
  only:
  - master